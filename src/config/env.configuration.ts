// configuration object so we dont have to type the values when needed
export const envConfiguration = {
  PORT: 'PORT',
  NODE_ENV: 'NODE_ENV',
  CONNECTION_STRING_ATLAS: 'CONNECTION_STRING_ATLAS',
};
